# Application
spring.application.name=Employee-Management-System

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/secondYSP?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=12345#Lmg
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# HikariCP Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=30000

# JWT Configuration
jwt.secret=SO8n5ydBFjBmXuWDCzjPfmR6x42cP+FusOlcbZa81T4=
jwt.tokenValidity=36000000
jwt.header=Authorization
jwt.prefix=Bearer

# Refresh Token Configuration
jwt.refresh-token.validity=604800000
jwt.refresh-token.secret=3t6w9z$B&E)H@McQfTjWnZr4u7x!A%C*F-JaNdRgUkXp2s5v8y/B

# CORS Configuration
cors.allowed-origins=http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Jackson Configuration (for JSON serialization)
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=UTC

# Server Error Handling
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never
server.error.include-exception=false

# Spring Security
spring.security.filter.order=1

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.EMS.Employee.Management.System=DEBUG

# Server Configuration
server.port=8080



# Production Settings (uncomment in production)
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=false
# logging.level.org.springframework.security=INFO
# logging.level.com.EMS.Employee.Management.System=INFO
# server.error.include-message=never
# server.error.include-binding-errors=never